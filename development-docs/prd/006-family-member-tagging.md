# PRD-006: FamilyMember 태그 기능

## 문서 정보
- **작성일**: 2026-01-06
- **상태**: 📝 작성중
- **우선순위**: 중간
- **영향 범위**: 백엔드 (be), 프론트엔드 (fe)

---

## 1. 배경 및 목적

### 1.1 현재 상황
- 사용자는 하나의 Family에 소속되어 구성원(FamilyMember) 정보를 관리
- Family 내 모든 구성원이 평면적으로 나열됨
- 특정 구성원들을 묶어서 관리하는 기능이 없음

### 1.2 문제점

| 문제 | 설명 |
|------|------|
| 구성원 분류 불가 | "친가 어른들", "외가", "조카들" 등으로 묶을 수 없음 |
| 필터링 불가 | 특정 그룹만 빠르게 조회할 수 없음 |
| 확장성 부족 | 향후 그룹 단위 알림, 메시지 전송 등 기능 추가 어려움 |

### 1.3 목표

1. **태그 기능 도입**: FamilyMember에 태그를 붙여 분류
2. **다대다 관계 지원**: 한 멤버에 여러 태그, 한 태그에 여러 멤버
3. **OWNER 전용 관리**: Family OWNER만 태그 생성/수정/삭제 가능
4. **향후 확장 기반 마련**: 태그별 필터링, 알림 등 기능의 기반

---

## 2. 도메인 용어 정의

| 용어 | 정의 | 비고 |
|-----|------|-----|
| 태그(Tag) | FamilyMember를 분류하기 위한 라벨 | Family 단위로 관리 |
| 태그 할당(Tagging) | FamilyMember에 태그를 연결하는 행위 | 다대다 관계 |
| 태그 해제(Untagging) | FamilyMember에서 태그를 제거하는 행위 | 연결만 끊음 |

---

## 3. 유스케이스

### UC-1: 태그 생성

- **액터**: Family OWNER
- **전제조건**: 사용자가 해당 Family의 OWNER 역할
- **기본 흐름**:
  1. 사용자가 태그 관리 화면 또는 태그 선택 모달에서 "새 태그 만들기" 선택
  2. 태그 이름 입력
  3. 시스템이 유효성 검증 (길이, 중복 등)
  4. 태그 생성 완료
- **예외 흐름**:
  - E1: 태그 수 제한 초과 (10개) → 에러 메시지 표시
  - E2: 이름 중복 → 에러 메시지 표시
  - E3: 이름 길이 초과 (10자) → 에러 메시지 표시

### UC-2: 태그 목록 조회

- **액터**: Family 구성원 (모든 역할)
- **전제조건**: 사용자가 해당 Family의 활성 구성원
- **기본 흐름**:
  1. 사용자가 태그 목록 요청
  2. 시스템이 Family의 모든 태그 반환
  3. 각 태그에 할당된 멤버 수 함께 표시
- **대안 흐름**:
  - A1: 태그가 없는 경우 → 빈 목록 반환

### UC-3: 태그 수정 (이름 변경)

- **액터**: Family OWNER
- **전제조건**: 사용자가 해당 Family의 OWNER 역할
- **기본 흐름**:
  1. 사용자가 수정할 태그 선택
  2. 새 이름 입력
  3. 시스템이 유효성 검증
  4. 태그 이름 변경 완료
- **예외 흐름**:
  - E1: 이름 중복 → 에러 메시지 표시
  - E2: 이름 길이 초과 → 에러 메시지 표시

### UC-4: 태그 삭제

- **액터**: Family OWNER
- **전제조건**: 사용자가 해당 Family의 OWNER 역할
- **기본 흐름**:
  1. 사용자가 삭제할 태그 선택
  2. 삭제 확인
  3. 시스템이 태그 삭제 (연결된 멤버들의 태그 연결도 자동 해제)
  4. 삭제 완료
- **사후조건**: 해당 태그에 연결된 모든 FamilyMember의 연결이 끊어짐

### UC-5: 멤버에 태그 할당

- **액터**: Family OWNER
- **전제조건**: 사용자가 해당 Family의 OWNER 역할
- **기본 흐름**:
  1. 멤버 목록에서 멤버 카드를 스와이프 (또는 상세에서 태그 버튼 클릭)
  2. "태그 추가" 버튼 클릭
  3. 태그 선택 모달 표시 (기존 태그 목록 + 새 태그 만들기)
  4. 태그 선택
  5. 멤버에 태그 할당 완료
- **대안 흐름**:
  - A1: 이미 할당된 태그 선택 → 토글 방식으로 해제
  - A2: 모달에서 새 태그 생성 후 바로 할당

### UC-6: 멤버에서 태그 해제

- **액터**: Family OWNER
- **전제조건**: 사용자가 해당 Family의 OWNER 역할
- **기본 흐름**:
  1. 태그 선택 모달에서 이미 할당된 태그 클릭 (체크 해제)
  2. 또는 멤버 상세에서 태그 뱃지의 X 버튼 클릭
  3. 멤버에서 태그 연결 해제
- **사후조건**: 태그 자체는 유지됨 (빈 태그 허용)

### UC-7: 태그별 멤버 필터링

- **액터**: Family 구성원 (모든 역할)
- **전제조건**: 사용자가 해당 Family의 활성 구성원
- **기본 흐름**:
  1. 멤버 목록 상단에서 태그 필터 칩 표시 (가로 스크롤)
  2. 태그 칩 클릭 시 해당 태그가 할당된 멤버만 표시
  3. 복수 태그 선택 시 OR 조건 (하나라도 포함된 멤버)
- **대안 흐름**:
  - A1: "전체" 칩 클릭 시 필터 해제
  - A2: 선택된 태그 칩 다시 클릭 시 해제 (토글)

---

## 4. 비즈니스 규칙

### BR-1: 태그 관리 권한
- **설명**: 태그 생성/수정/삭제는 Family OWNER만 가능
- **적용 시점**: 태그 CUD 요청 시
- **위반 시 처리**: 403 Forbidden 반환

### BR-2: 태그 수 제한
- **설명**: 한 Family당 최대 10개의 태그만 생성 가능
- **적용 시점**: 태그 생성 요청 시
- **위반 시 처리**: 400 Bad Request 반환 (T001)

### BR-3: 태그명 고유성
- **설명**: 같은 Family 내에서 태그 이름은 중복 불가
- **적용 시점**: 태그 생성/수정 요청 시
- **위반 시 처리**: 400 Bad Request 반환 (T002)

### BR-4: 태그명 길이 제한
- **설명**: 태그 이름은 1자 이상 10자 이하
- **적용 시점**: 태그 생성/수정 요청 시
- **위반 시 처리**: 400 Bad Request 반환 (T003)

### BR-5: 태그명 허용 문자
- **설명**: 한글, 영문, 숫자, 이모지, 공백만 허용
- **적용 시점**: 태그 생성/수정 요청 시
- **위반 시 처리**: 400 Bad Request 반환 (T004)

### BR-6: 태그 할당 권한
- **설명**: 멤버에 태그 할당/해제는 Family OWNER만 가능
- **적용 시점**: 태그 할당/해제 요청 시
- **위반 시 처리**: 403 Forbidden 반환

### BR-7: 멤버당 태그 수 제한
- **설명**: 한 멤버에 할당 가능한 태그 수는 최대 10개
- **적용 시점**: 태그 할당 요청 시
- **위반 시 처리**: 400 Bad Request 반환 (T008)

### BR-8: 빈 태그 허용
- **설명**: 멤버가 0명인 태그도 유지 가능
- **적용 시점**: 태그 삭제 판단 시
- **비고**: 미리 태그를 만들어두고 나중에 할당 가능

### BR-9: 태그 삭제 시 연결 해제
- **설명**: 태그 삭제 시 해당 태그에 연결된 모든 멤버의 연결만 끊고, 멤버 자체는 영향 없음
- **적용 시점**: 태그 삭제 요청 시
- **비고**: CASCADE DELETE on FamilyMemberTag

### BR-10: 태그 색상
- **설명**: 태그 생성 시 색상 팔레트에서 랜덤 배정, 이후 변경 가능
- **적용 시점**: 태그 생성/수정 요청 시
- **비고**: 노션 스타일 색상 팔레트 (10가지 색상)

### BR-11: 태그 정렬
- **설명**: 태그 목록은 가나다순(이름 오름차순)으로 정렬
- **적용 시점**: 태그 목록 조회 시
- **비고**: 한글 → 영문 → 숫자 순

---

## 5. 상태 전이

태그 자체는 상태 전이가 없음 (생성 → 삭제만 존재)

---

## 6. UI/UX 설계

### 6.1 색상 팔레트 (노션 스타일)

태그 생성 시 아래 팔레트에서 랜덤 배정, 이후 사용자가 변경 가능:

| 색상명 | HEX | 용도 예시 |
|--------|-----|----------|
| Light Gray | `#E3E2E0` | 기본 |
| Brown | `#EEE0DA` | |
| Orange | `#FADEC9` | |
| Yellow | `#FDECC8` | |
| Green | `#DBEDDB` | |
| Blue | `#D3E5EF` | |
| Purple | `#E8DEEE` | |
| Pink | `#F5E0E9` | |
| Red | `#FFE2DD` | |

### 6.2 멤버 카드 스와이프 액션

```text
┌─────────────────────────────────────────────────────────────────┐
│ [아바타] 홍길동 (35)        1990.12.25    [>] │ 태그 추가 │ ... │
└─────────────────────────────────────────────────────────────────┘
                                                ↑ 스와이프 시 노출
                                                  (파란색 배경)
```

### 6.3 멤버 클릭 시 상세 모달

**현재 문제**: HomePage에서 멤버 클릭 시 404 발생
**해결**: 멤버 클릭 시 상세 페이지 대신 **모달**을 띄움

```text
┌─────────────────────────────────────────┐
│              홍길동              [X]    │
├─────────────────────────────────────────┤
│                                         │
│  [아바타 크게]                           │
│                                         │
│  생일: 1990.12.25 (35세)                │
│  관계: 아버지                           │
│                                         │
│  태그:                                  │
│  [🔵 친가 어른들] [🟢 조카들]            │
│                                         │
├─────────────────────────────────────────┤
│  [태그 수정]  [닫기]                     │
└─────────────────────────────────────────┘
       ↑ 태그 뱃지에 색상 표시
       ↑ OWNER만 "태그 수정" 버튼 노출
```

### 6.4 태그 선택 모달

```text
┌─────────────────────────────────────────┐
│              태그 선택            [취소] │
├─────────────────────────────────────────┤
│                                         │
│  [🔵] ☑️ 친가 어른들               3  │
│  [🟠] ☐ 외가                      5  │
│  [🟢] ☑️ 조카들                   2  │
│  [🟣] ☐ 이모네                    4  │
│                                         │
├─────────────────────────────────────────┤
│  ➕ 새 태그 만들기                       │
└─────────────────────────────────────────┘
       ↑ 색상 아이콘 + 체크박스로 다중 선택/해제
       ↑ 숫자는 해당 태그에 할당된 멤버 수
       ↑ 가나다순 정렬
```

### 6.5 멤버 카드에 태그 표시

```text
┌─────────────────────────────────────────────────────────────────┐
│ [아바타] 홍길동 (35)        1990.12.25                      [>] │
│          [🔵 친가 어른들] [🟢 조카들]                           │
└─────────────────────────────────────────────────────────────────┘
           ↑ 태그 뱃지 (각 태그 색상 배경, 작은 글씨)
```

### 6.6 태그 필터 (멤버 목록 상단)

```text
┌─────────────────────────────────────────────────────────────────┐
│  [전체] [🔵 친가 어른들] [🟠 외가] [🟢 조카들] [🟣 이모네] →    │
└─────────────────────────────────────────────────────────────────┘
   ↑ 가로 스크롤 가능
   ↑ 선택된 칩은 테두리 강조 또는 체크 아이콘
   ↑ 복수 선택 가능 (OR 조건)

필터 적용 시:
┌─────────────────────────────────────────────────────────────────┐
│  [전체] [🔵 친가 어른들 ✓] [🟠 외가] [🟢 조카들 ✓] [🟣 이모네]  │
├─────────────────────────────────────────────────────────────────┤
│  2개 태그 선택됨 · 5명                                          │
├─────────────────────────────────────────────────────────────────┤
│  [아바타] 홍길동 (35)        1990.12.25                     [>] │
│           [🔵 친가 어른들] [🟢 조카들]                          │
│  ...                                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 6.7 태그 관리 화면 (설정에서 접근)

```text
┌─────────────────────────────────────────┐
│  [<] 태그 관리                          │
├─────────────────────────────────────────┤
│                                         │
│  [🔵] 친가 어른들              3명  [...│
│  [🟠] 외가                    5명  [...│
│  [🟢] 조카들                  2명  [...│
│  [🟣] 이모네                  4명  [...│
│                                         │
│  (4/10개 사용중)                         │
│                                         │
├─────────────────────────────────────────┤
│  ➕ 새 태그 만들기                       │
└─────────────────────────────────────────┘
       ↑ [...] 클릭 시: 색상 변경, 이름 변경, 삭제 메뉴
       ↑ 가나다순 정렬
```

---

## 7. API 명세

### 7.1 태그 목록 조회

- **Method**: `GET`
- **Path**: `/api/families/{familyId}/tags`
- **인증**: 필요
- **권한**: Family 활성 구성원

#### 응답

**성공 (200)**
```json
{
  "tags": [
    {
      "id": 1,
      "name": "친가 어른들",
      "color": "#D3E5EF",
      "memberCount": 3,
      "createdAt": "2026-01-06T10:00:00"
    },
    {
      "id": 2,
      "name": "외가",
      "color": "#FADEC9",
      "memberCount": 5,
      "createdAt": "2026-01-06T10:05:00"
    }
  ],
  "totalCount": 2,
  "maxCount": 10
}
```

### 7.2 태그 생성

- **Method**: `POST`
- **Path**: `/api/families/{familyId}/tags`
- **인증**: 필요
- **권한**: Family OWNER

#### 요청

```json
{
  "name": "친가 어른들"
}
```

| 필드 | 타입 | 필수 | 설명 | 제약조건 |
|-----|-----|-----|------|---------|
| name | String | Y | 태그 이름 | 1~10자, 한글/영문/숫자/이모지/공백 |

> **Note**: 색상은 서버에서 COLOR_PALETTE 중 랜덤 배정

#### 응답

**성공 (201)**
```json
{
  "id": 1,
  "name": "친가 어른들",
  "color": "#D3E5EF",
  "memberCount": 0,
  "createdAt": "2026-01-06T10:00:00"
}
```

**에러**
| 상태코드 | 코드 | 상황 | 메시지 |
|---------|-----|------|--------|
| 400 | T001 | 태그 수 제한 초과 | 태그는 최대 10개까지 생성할 수 있습니다 |
| 400 | T002 | 이름 중복 | 이미 존재하는 태그 이름입니다 |
| 400 | T003 | 이름 길이 초과 | 태그 이름은 1~10자여야 합니다 |
| 400 | T004 | 허용되지 않는 문자 | 태그 이름에 허용되지 않는 문자가 포함되어 있습니다 |
| 403 | F003 | 권한 없음 | 태그를 관리할 권한이 없습니다 |

### 7.3 태그 수정

- **Method**: `PUT`
- **Path**: `/api/families/{familyId}/tags/{tagId}`
- **인증**: 필요
- **권한**: Family OWNER

#### 요청

```json
{
  "name": "친가",
  "color": "#FFE2DD"
}
```

| 필드 | 타입 | 필수 | 설명 | 제약조건 |
|-----|-----|-----|------|---------|
| name | String | Y | 태그 이름 | 1~10자, 한글/영문/숫자/이모지/공백 |
| color | String | N | 태그 색상 | COLOR_PALETTE 중 하나 |

#### 응답

**성공 (200)**
```json
{
  "id": 1,
  "name": "친가",
  "color": "#FFE2DD",
  "memberCount": 3,
  "createdAt": "2026-01-06T10:00:00",
  "modifiedAt": "2026-01-06T11:00:00"
}
```

### 7.4 태그 삭제

- **Method**: `DELETE`
- **Path**: `/api/families/{familyId}/tags/{tagId}`
- **인증**: 필요
- **권한**: Family OWNER

#### 응답

**성공 (204)**: No Content

**에러**
| 상태코드 | 코드 | 상황 | 메시지 |
|---------|-----|------|--------|
| 404 | T005 | 태그 없음 | 존재하지 않는 태그입니다 |
| 403 | F003 | 권한 없음 | 태그를 관리할 권한이 없습니다 |

### 7.5 멤버에 태그 할당/해제

- **Method**: `PUT`
- **Path**: `/api/families/{familyId}/members/{memberId}/tags`
- **인증**: 필요
- **권한**: Family OWNER

#### 요청

```json
{
  "tagIds": [1, 3, 5]
}
```

| 필드 | 타입 | 필수 | 설명 |
|-----|-----|-----|------|
| tagIds | Long[] | Y | 할당할 태그 ID 목록 (빈 배열이면 모든 태그 해제) |

#### 응답

**성공 (200)**
```json
{
  "memberId": 1,
  "memberName": "홍길동",
  "tags": [
    { "id": 1, "name": "친가 어른들", "color": "#D3E5EF" },
    { "id": 3, "name": "조카들", "color": "#DBEDDB" },
    { "id": 5, "name": "이모네", "color": "#E8DEEE" }
  ]
}
```

### 7.6 멤버 목록 조회 (태그 정보 포함) - 기존 API 확장

- **Method**: `GET`
- **Path**: `/api/families/{familyId}/home/members`
- **변경사항**: 응답에 `tags` 필드 추가

#### 응답 (확장)

```json
{
  "members": [
    {
      "memberId": 1,
      "memberName": "홍길동",
      "memberBirthday": "1990-12-25T00:00:00",
      "memberBirthdayType": "SOLAR",
      "tags": [
        { "id": 1, "name": "친가 어른들", "color": "#D3E5EF" },
        { "id": 3, "name": "조카들", "color": "#DBEDDB" }
      ],
      ...
    }
  ]
}
```

### 7.7 태그별 멤버 조회 (향후)

- **Method**: `GET`
- **Path**: `/api/families/{familyId}/tags/{tagId}/members`
- **인증**: 필요
- **권한**: Family 활성 구성원

#### 응답

```json
{
  "tag": {
    "id": 1,
    "name": "친가 어른들",
    "color": "#D3E5EF"
  },
  "members": [
    {
      "memberId": 1,
      "memberName": "홍길동",
      ...
    }
  ],
  "totalCount": 3
}
```

---

## 8. 데이터베이스 설계

### 8.1 family_member_tag 테이블 (신규)

```sql
CREATE TABLE family_member_tag (
    id BIGSERIAL PRIMARY KEY,
    family_id BIGINT NOT NULL,
    name VARCHAR(10) NOT NULL,
    color VARCHAR(7) NOT NULL DEFAULT '#E3E2E0',
    created_by BIGINT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    modified_by BIGINT,
    modified_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_tag_family FOREIGN KEY (family_id) REFERENCES family(id),
    CONSTRAINT uk_family_tag_name UNIQUE (family_id, name)
);

CREATE INDEX idx_tag_family_id ON family_member_tag(family_id);

COMMENT ON TABLE family_member_tag IS 'FamilyMember 분류를 위한 태그';
COMMENT ON COLUMN family_member_tag.name IS '태그 이름 (1~10자)';
COMMENT ON COLUMN family_member_tag.color IS '태그 색상 HEX 코드 (예: #E3E2E0)';
```

### 8.2 family_member_tag_mapping 테이블 (신규)

```sql
CREATE TABLE family_member_tag_mapping (
    id BIGSERIAL PRIMARY KEY,
    tag_id BIGINT NOT NULL,
    member_id BIGINT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_mapping_tag FOREIGN KEY (tag_id) REFERENCES family_member_tag(id) ON DELETE CASCADE,
    CONSTRAINT fk_mapping_member FOREIGN KEY (member_id) REFERENCES family_member(id) ON DELETE CASCADE,
    CONSTRAINT uk_tag_member UNIQUE (tag_id, member_id)
);

CREATE INDEX idx_mapping_tag_id ON family_member_tag_mapping(tag_id);
CREATE INDEX idx_mapping_member_id ON family_member_tag_mapping(member_id);

COMMENT ON TABLE family_member_tag_mapping IS 'FamilyMember와 Tag의 다대다 매핑';
```

### 8.3 ERD

```
┌─────────────┐       ┌────────────────────┐       ┌─────────────────────────┐
│   family    │       │ family_member_tag  │       │ family_member_tag_mapping│
├─────────────┤       ├────────────────────┤       ├─────────────────────────┤
│ id (PK)     │◄──────│ family_id (FK)     │       │ id (PK)                 │
│ name        │       │ id (PK)            │◄──────│ tag_id (FK)             │
│ ...         │       │ name               │       │ member_id (FK)          │────►┌──────────────┐
└─────────────┘       │ color              │       │ created_at              │     │ family_member│
                      │ created_at         │       └─────────────────────────┘     ├──────────────┤
                      │ ...                │                                       │ id (PK)      │
                      └────────────────────┘                                       │ family_id    │
                                                                                   │ ...          │
                                                                                   └──────────────┘
```

---

## 9. 도메인 설계

### 9.1 FamilyMemberTag (신규 도메인)

```java
public class FamilyMemberTag {
    private final Long id;
    private final Long familyId;
    private final String name;
    private final String color;  // 색상 HEX 코드 (예: #E3E2E0)
    private final Long createdBy;
    private final LocalDateTime createdAt;
    private final Long modifiedBy;
    private final LocalDateTime modifiedAt;

    // 팩토리 메서드
    public static FamilyMemberTag newTag(Long familyId, String name, Long createdBy) {
        // color는 COLOR_PALETTE에서 랜덤 선택
        ...
    }
    public static FamilyMemberTag withId(Long id, Long familyId, String name, String color, ...) { ... }

    // 비즈니스 메서드
    public FamilyMemberTag rename(String newName, Long modifiedBy) { ... }
    public FamilyMemberTag changeColor(String newColor, Long modifiedBy) { ... }

    // 색상 팔레트 상수
    public static final List<String> COLOR_PALETTE = List.of(
        "#E3E2E0", "#EEE0DA", "#FADEC9", "#FDECC8", "#DBEDDB",
        "#D3E5EF", "#E8DEEE", "#F5E0E9", "#FFE2DD"
    );
}
```

### 9.2 FamilyMemberTagMapping (신규 도메인)

```java
public class FamilyMemberTagMapping {
    private final Long id;
    private final Long tagId;
    private final Long memberId;
    private final LocalDateTime createdAt;

    // 팩토리 메서드
    public static FamilyMemberTagMapping newMapping(Long tagId, Long memberId) { ... }
    public static FamilyMemberTagMapping withId(Long id, Long tagId, Long memberId, LocalDateTime createdAt) { ... }
}
```

---

## 10. 예외 케이스 상세

### 10.1 에러 코드 정의

| 코드 | 상황 | HTTP 상태 | 메시지 |
|-----|------|----------|--------|
| T001 | 태그 수 제한 초과 | 400 | 태그는 최대 10개까지 생성할 수 있습니다 |
| T002 | 태그 이름 중복 | 400 | 이미 존재하는 태그 이름입니다 |
| T003 | 태그 이름 길이 | 400 | 태그 이름은 1~10자여야 합니다 |
| T004 | 허용되지 않는 문자 | 400 | 태그 이름에 허용되지 않는 문자가 포함되어 있습니다 |
| T005 | 태그 없음 | 404 | 존재하지 않는 태그입니다 |
| T006 | 다른 Family의 태그 | 400 | 해당 Family의 태그가 아닙니다 |
| T007 | 다른 Family의 멤버 | 400 | 해당 Family의 멤버가 아닙니다 |
| T008 | 멤버당 태그 수 초과 | 400 | 한 멤버에 최대 10개의 태그만 할당할 수 있습니다 |
| T009 | 유효하지 않은 색상 | 400 | 허용되지 않는 색상입니다 |
| F001 | Family 없음 | 404 | 존재하지 않는 Family입니다 |
| F002 | Family 접근 권한 없음 | 403 | 해당 Family에 접근할 권한이 없습니다 |
| F003 | OWNER 권한 없음 | 403 | 태그를 관리할 권한이 없습니다 |
| F009 | 멤버 없음 | 404 | 존재하지 않는 멤버입니다 |

### 10.2 API별 예외 케이스

#### POST /api/families/{familyId}/tags (태그 생성)

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| familyId가 존재하지 않음 | F001 | 404 | Service |
| 사용자가 Family 구성원이 아님 | F002 | 403 | Service |
| 사용자가 OWNER가 아님 | F003 | 403 | Service |
| name이 null 또는 빈 문자열 | T003 | 400 | Command (Jakarta Validation) |
| name이 10자 초과 | T003 | 400 | Command (Jakarta Validation) |
| name에 허용되지 않는 문자 포함 | T004 | 400 | Command (Pattern Validation) |
| Family에 이미 10개 태그 존재 | T001 | 400 | Service |
| 동일한 이름의 태그 존재 | T002 | 400 | Service |

#### GET /api/families/{familyId}/tags (태그 목록 조회)

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| familyId가 존재하지 않음 | F001 | 404 | Service |
| 사용자가 Family 구성원이 아님 | F002 | 403 | Service |

#### PUT /api/families/{familyId}/tags/{tagId} (태그 수정)

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| familyId가 존재하지 않음 | F001 | 404 | Service |
| 사용자가 Family 구성원이 아님 | F002 | 403 | Service |
| 사용자가 OWNER가 아님 | F003 | 403 | Service |
| tagId가 존재하지 않음 | T005 | 404 | Service |
| 태그가 해당 Family의 것이 아님 | T006 | 400 | Service |
| name이 null 또는 빈 문자열 | T003 | 400 | Command |
| name이 10자 초과 | T003 | 400 | Command |
| name에 허용되지 않는 문자 포함 | T004 | 400 | Command |
| 동일한 이름의 태그 존재 (본인 제외) | T002 | 400 | Service |

#### DELETE /api/families/{familyId}/tags/{tagId} (태그 삭제)

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| familyId가 존재하지 않음 | F001 | 404 | Service |
| 사용자가 Family 구성원이 아님 | F002 | 403 | Service |
| 사용자가 OWNER가 아님 | F003 | 403 | Service |
| tagId가 존재하지 않음 | T005 | 404 | Service |
| 태그가 해당 Family의 것이 아님 | T006 | 400 | Service |

#### PUT /api/families/{familyId}/members/{memberId}/tags (멤버 태그 할당)

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| familyId가 존재하지 않음 | F001 | 404 | Service |
| 사용자가 Family 구성원이 아님 | F002 | 403 | Service |
| 사용자가 OWNER가 아님 | F003 | 403 | Service |
| memberId가 존재하지 않음 | F009 | 404 | Service |
| 멤버가 해당 Family의 것이 아님 | T007 | 400 | Service |
| tagIds 중 존재하지 않는 태그 | T005 | 404 | Service |
| tagIds 중 다른 Family의 태그 | T006 | 400 | Service |
| tagIds가 10개 초과 | T008 | 400 | Service |

#### PUT /api/families/{familyId}/tags/{tagId} (태그 수정) - 색상 검증 추가

| 상황 | 에러코드 | HTTP | 검증 위치 |
|------|---------|------|----------|
| color가 COLOR_PALETTE에 없음 | T009 | 400 | Command (Pattern Validation) |

---

## 11. 기술적 고려사항

### 11.1 성능 최적화

#### N+1 쿼리 방지

**문제**: 멤버 목록 조회 시 각 멤버별로 태그를 조회하면 N+1 문제 발생

```
-- 비효율적 (N+1)
SELECT * FROM family_member WHERE family_id = ?  -- 1회
SELECT * FROM family_member_tag_mapping WHERE member_id = ?  -- N회 (멤버 수만큼)
```

**해결책**: 두 가지 방식 중 선택

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **Batch IN Query** | 멤버 ID 목록으로 한 번에 조회 후 메모리에서 매핑 | 구현 간단, 유연함 | 대량 데이터 시 메모리 사용 |
| **JOIN FETCH** | 멤버 조회 시 태그까지 한 번에 JOIN | 쿼리 1회 | Cartesian Product 주의 |

**권장**: Batch IN Query 방식
```java
// 1. 멤버 목록 조회
List<FamilyMember> members = findAllByFamilyId(familyId);

// 2. 멤버 ID 목록 추출
List<Long> memberIds = members.stream().map(FamilyMember::getId).toList();

// 3. 태그 매핑 한 번에 조회
List<FamilyMemberTagMapping> mappings = findAllByMemberIdIn(memberIds);

// 4. 메모리에서 그룹핑
Map<Long, List<Tag>> tagsByMemberId = groupByMemberId(mappings);
```

#### 태그별 멤버 수 카운트

**옵션 1**: 실시간 COUNT 쿼리 (현재 권장)
- 장점: 데이터 정합성 보장
- 단점: 조회마다 COUNT 발생
- 적용: 태그 수가 최대 10개이므로 성능 영향 미미

**옵션 2**: Denormalization (memberCount 컬럼)
- 장점: 조회 성능 향상
- 단점: 매핑 변경 시 동기화 필요, 정합성 위험
- 적용: 향후 태그 수 제한 증가 또는 성능 이슈 발생 시 고려

#### 인덱스 전략

```sql
-- family_member_tag
CREATE INDEX idx_tag_family_id ON family_member_tag(family_id);
-- UNIQUE 제약조건이 있으므로 (family_id, name) 복합 인덱스 자동 생성

-- family_member_tag_mapping
CREATE INDEX idx_mapping_tag_id ON family_member_tag_mapping(tag_id);
CREATE INDEX idx_mapping_member_id ON family_member_tag_mapping(member_id);
-- UNIQUE 제약조건이 있으므로 (tag_id, member_id) 복합 인덱스 자동 생성
```

---

### 11.2 동시성 및 트랜잭션

#### 태그 수 제한 Race Condition

**문제**: 동시에 태그 생성 요청 시 10개 제한을 초과할 수 있음

```
시점 T1: 사용자 A가 태그 생성 요청 (현재 9개)
시점 T2: 사용자 B가 태그 생성 요청 (현재 9개)
시점 T3: 사용자 A 검증 통과 (9 < 10)
시점 T4: 사용자 B 검증 통과 (9 < 10)
시점 T5: 사용자 A 저장 완료 (10개)
시점 T6: 사용자 B 저장 완료 (11개!) ← 제한 초과
```

**해결책**: DB 레벨 제약 + 애플리케이션 재검증

```sql
-- 방법 1: CHECK 제약조건 (PostgreSQL)
-- 트리거로 구현 필요, 복잡함

-- 방법 2: 애플리케이션에서 비관적 락
SELECT * FROM family_member_tag WHERE family_id = ? FOR UPDATE;
```

**권장**: 현재는 애플리케이션 레벨 검증으로 충분
- OWNER만 태그 생성 가능 (동시 요청 가능성 낮음)
- 제한 초과 시 사용자에게 재시도 안내

#### 태그 이름 중복

**해결책**: UNIQUE 제약조건으로 DB 레벨에서 보장
```sql
CONSTRAINT uk_family_tag_name UNIQUE (family_id, name)
```
- 애플리케이션에서 먼저 검증하여 사용자 친화적 에러 메시지 제공
- DB 제약조건은 최후 방어선

#### 멤버 태그 할당 트랜잭션

**전략**: 전체 교체 방식 (Replace All)
```java
@Transactional
public void modifyMemberTags(Long memberId, List<Long> tagIds) {
    // 1. 기존 매핑 전체 삭제
    deleteAllByMemberId(memberId);

    // 2. 새 매핑 전체 저장
    saveAll(tagIds.stream()
        .map(tagId -> FamilyMemberTagMapping.newMapping(tagId, memberId))
        .toList());
}
```

**장점**: 구현 단순, 멱등성 보장
**단점**: 매핑이 많을 경우 비효율적 (최대 10개이므로 무관)

---

### 11.3 데이터 일관성

#### CASCADE DELETE 선택 이유

| 항목 | CASCADE DELETE | 애플리케이션 삭제 |
|------|----------------|-----------------|
| 구현 복잡도 | 낮음 | 높음 (삭제 순서 관리) |
| 성능 | DB 최적화 | 추가 쿼리 필요 |
| 트랜잭션 | DB가 보장 | 애플리케이션이 보장 |
| 유연성 | 낮음 | 높음 (소프트 삭제 등) |

**결정**: CASCADE DELETE 사용
- 태그 삭제 시 매핑도 함께 삭제되어야 함 (비즈니스 요구사항)
- 소프트 삭제 불필요 (태그 복구 요구사항 없음)

#### Soft Delete 미적용 이유

- 태그는 단순 분류 목적, 삭제 후 복구 요구사항 없음
- 삭제된 태그 히스토리 불필요
- 향후 필요 시 `deleted`, `deleted_at` 컬럼 추가로 대응 가능

---

### 11.4 확장성 고려

#### 태그 속성 추가 대비

향후 태그에 색상, 아이콘, 순서 등 속성 추가 가능성:

```java
// 현재
public class FamilyMemberTag {
    private Long id;
    private Long familyId;
    private String name;
}

// 향후 확장
public class FamilyMemberTag {
    private Long id;
    private Long familyId;
    private String name;
    private String color;        // 추가
    private Integer sortOrder;   // 추가
    private String iconUrl;      // 추가
}
```

**설계 포인트**:
- 새 필드 추가 시 기존 API 하위 호환 유지 (nullable)
- Response DTO는 필요한 필드만 노출

#### 태그 수 제한 변경 대비

현재 10개 제한을 상수로 관리:
```java
public class FamilyMemberTagPolicy {
    public static final int MAX_TAGS_PER_FAMILY = 10;
}
```

향후 Family별 다른 제한 필요 시:
```java
// Family 엔티티에 maxTags 필드 추가
// 또는 별도 설정 테이블에서 조회
```

---

### 11.5 캐싱 전략 (향후)

태그 목록은 자주 변경되지 않으므로 캐싱 후보:

| 캐시 대상 | 캐시 키 | TTL | 무효화 시점 |
|----------|--------|-----|------------|
| 태그 목록 | `tags:family:{familyId}` | 5분 | 태그 CUD 시 |
| 태그별 멤버 수 | `tag:count:{tagId}` | 1분 | 매핑 변경 시 |

**현재**: 캐싱 미적용 (데이터 규모가 작음)
**향후**: Redis 캐싱 고려 (Family당 태그 10개, 조회 빈도 높을 경우)

---

### 11.6 API 설계 결정

#### 멤버 태그 할당: PUT vs PATCH

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **PUT (전체 교체)** | tagIds 전체를 새로 설정 | 멱등성, 구현 단순 | 클라이언트가 전체 목록 알아야 함 |
| PATCH (부분 수정) | 추가/삭제할 태그만 전달 | 부분 업데이트 가능 | 복잡한 요청 형식 |

**결정**: PUT (전체 교체)
- 태그 수가 최대 10개로 적음
- 클라이언트(모달)에서 체크박스로 전체 선택 상태 관리
- 멱등성 보장으로 재시도 안전

#### 벌크 연산 필요성

**현재**: 개별 멤버 단위 태그 할당
**향후 고려**: 여러 멤버에 동시 태그 할당

```
// 현재
PUT /api/families/{familyId}/members/{memberId}/tags

// 향후 (필요 시)
PUT /api/families/{familyId}/tags/{tagId}/members
{ "memberIds": [1, 2, 3] }
```

---

### 11.7 에러 처리 및 로깅

#### 에러 코드 체계

기존 에러 코드와의 일관성:
- `F0XX`: Family 관련 (F001, F002, F003, F009 등 기존 코드 재사용)
- `T0XX`: Tag 관련 (신규 코드 T001~T007)

#### 로깅 포인트

| 이벤트 | 로그 레벨 | 포함 정보 |
|--------|----------|----------|
| 태그 생성 | INFO | familyId, tagName, createdBy |
| 태그 삭제 | INFO | familyId, tagId, deletedBy |
| 권한 검증 실패 | WARN | familyId, userId, requiredRole |
| 태그 수 제한 초과 | WARN | familyId, currentCount |

---

### 11.8 테스트 전략

#### 단위 테스트 커버리지

| 영역 | 테스트 항목 |
|------|-----------|
| Domain | 팩토리 메서드, rename 불변성, 검증 로직 |
| Service | 권한 검증, 비즈니스 규칙 (제한, 중복), 예외 케이스 |
| Adapter | Repository 메서드, 변환 로직 |

#### 통합 테스트 시나리오

1. **권한 테스트**: OWNER 외 사용자 태그 생성 시도 → 403
2. **제한 테스트**: 11번째 태그 생성 시도 → T001
3. **중복 테스트**: 동일 이름 태그 생성 시도 → T002
4. **CASCADE 테스트**: 태그 삭제 시 매핑 자동 삭제 확인
5. **동시성 테스트**: 동시 태그 생성 요청 시 제한 준수 확인

---

## 12. 제약사항 요약

### 기술적 제약
- 태그 삭제 시 매핑 테이블은 CASCADE DELETE로 자동 삭제
- 멤버 삭제 시 매핑 테이블은 CASCADE DELETE로 자동 삭제
- 태그 이름 중복은 DB UNIQUE 제약조건으로 최종 보장

### 성능 요구사항
- 태그 목록 조회: 100ms 이내 (Family당 최대 10개)
- 태그 생성/수정/삭제: 200ms 이내
- 멤버에 태그 할당: 200ms 이내
- 멤버 목록 + 태그 조회: N+1 방지 필수

### 보안 요구사항
- 모든 API는 인증 필요 (JWT)
- 태그 CUD는 OWNER 권한 검증 필수
- 태그 조회는 Family 구성원 권한 검증
- Family 접근 권한 검증 (다른 Family의 태그 접근 불가)

---

## 13. 영향 범위

### 13.1 백엔드 신규 파일

| 파일 | 설명 |
|------|------|
| `core/family/domain/FamilyMemberTag.java` | 태그 도메인 |
| `core/family/domain/FamilyMemberTagMapping.java` | 태그-멤버 매핑 도메인 |
| `core/family/adapter/out/persistence/FamilyMemberTagJpaEntity.java` | 태그 JPA 엔티티 |
| `core/family/adapter/out/persistence/FamilyMemberTagMappingJpaEntity.java` | 매핑 JPA 엔티티 |
| `core/family/adapter/out/persistence/FamilyMemberTagJpaRepository.java` | 태그 Repository |
| `core/family/adapter/out/persistence/FamilyMemberTagMappingJpaRepository.java` | 매핑 Repository |
| `core/family/adapter/out/persistence/FamilyMemberTagAdapter.java` | 태그 Adapter |
| `core/family/application/port/in/SaveFamilyMemberTagUseCase.java` | 태그 생성 UseCase |
| `core/family/application/port/in/FindFamilyMemberTagUseCase.java` | 태그 조회 UseCase |
| `core/family/application/port/in/ModifyFamilyMemberTagUseCase.java` | 태그 수정 UseCase |
| `core/family/application/port/in/DeleteFamilyMemberTagUseCase.java` | 태그 삭제 UseCase |
| `core/family/application/port/in/ModifyMemberTagsUseCase.java` | 멤버 태그 할당 UseCase |
| `core/family/application/service/SaveFamilyMemberTagService.java` | 태그 생성 Service |
| `core/family/application/service/FindFamilyMemberTagService.java` | 태그 조회 Service |
| `core/family/application/service/ModifyFamilyMemberTagService.java` | 태그 수정 Service |
| `core/family/application/service/DeleteFamilyMemberTagService.java` | 태그 삭제 Service |
| `core/family/application/service/ModifyMemberTagsService.java` | 멤버 태그 할당 Service |
| `core/family/adapter/in/web/FamilyMemberTagController.java` | 태그 Controller |
| DB 마이그레이션 | `V{N}__add_family_member_tag.sql` |

### 13.2 백엔드 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `core/family/adapter/in/response/FamilyMemberWithRelationshipResponse.java` | tags 필드 추가 |
| `core/family/application/service/FindFamilyHomeMemberService.java` | 태그 정보 포함 조회 |

### 13.3 프론트엔드 신규/수정 파일

| 파일 | 설명 |
|------|------|
| `components/family/TagSelectModal.tsx` | 태그 선택 모달 |
| `components/family/TagBadge.tsx` | 태그 뱃지 컴포넌트 (색상 지원) |
| `components/family/TagFilter.tsx` | 태그 필터 칩 컴포넌트 (가로 스크롤) |
| `components/family/TagManagement.tsx` | 태그 관리 화면 |
| `components/family/ColorPicker.tsx` | 색상 팔레트 선택 컴포넌트 |
| `components/family/MemberDetailModal.tsx` | 멤버 상세 정보 모달 |
| `hooks/queries/useTagQueries.ts` | 태그 관련 React Query 훅 |
| `api/services/tagService.ts` | 태그 API 서비스 |
| `types/tag.ts` | 태그 타입 정의 (색상 포함) |
| `pages/HomePage.tsx` | 멤버 카드에 스와이프/태그 표시 + 클릭 시 모달 + 필터 |
| `components/family/FamilyMemberCard.tsx` | 태그 뱃지 표시 |

---

## 14. 스프린트 계획

> **중요**: 각 스프린트는 **개별 PR**로 올립니다. 스프린트 완료 후 `commit` → `create-pr` 스킬 사용.

### 14.0 스프린트 요약

| Sprint | 범위 | 핵심 결과물 | 스킬 |
|--------|------|------------|------|
| **1-BE** | 태그 CRUD 백엔드 | 태그 API 완성 | `core-tdd` → `infra-tdd` → `presentation-tdd` |
| **1-FE** | 태그 관리 화면 | 설정에서 태그 관리 | - |
| **2-BE** | 멤버-태그 연결 백엔드 | 멤버 태그 할당 API | `core-tdd` → `infra-tdd` → `presentation-tdd` |
| **2-FE** | 멤버-태그 연결 프론트 | 모달에서 태그 선택/표시 | - |
| **3** | 스와이프 UX | 스와이프 → 태그 추가 | - |
| **4** | 필터링 | 태그별 필터 | - |

**의존성**:
```
Sprint 1-BE → Sprint 1-FE
     ↓
Sprint 2-BE → Sprint 2-FE → Sprint 3
                   ↓
              Sprint 4
```

---

### 14.1 Sprint 1-BE: 태그 CRUD 백엔드

**목표**: 태그 생성/조회/수정/삭제 API 완성

| 단계 | 작업 내용 | 스킬 |
|------|----------|------|
| 1 | FamilyMemberTag 도메인 | `core-tdd` |
| 2 | FamilyMemberTagJpaEntity, Repository | `infra-tdd` |
| 3 | FamilyMemberTagAdapter | `infra-tdd` |
| 4 | SaveFamilyMemberTagCommand/UseCase/Service | `core-tdd` |
| 5 | FindFamilyMemberTagQuery/UseCase/Service | `core-tdd` |
| 6 | ModifyFamilyMemberTagCommand/UseCase/Service | `core-tdd` |
| 7 | DeleteFamilyMemberTagUseCase/Service | `core-tdd` |
| 8 | FamilyMemberTagController (CRUD) | `presentation-tdd` |
| 9 | DB 마이그레이션 (family_member_tag) | 수동 |

**체크리스트**:
- [x] 도메인 테스트 통과
- [x] 서비스 테스트 통과
- [x] 컨트롤러 테스트 통과 (REST Docs)
- [x] `./gradlew test` 전체 통과
- [x] `./gradlew build` 성공
- [x] PR 생성 (`create-pr` 스킬) - PR #27

---

### 14.2 Sprint 1-FE: 태그 관리 화면

**목표**: 설정에서 태그 CRUD 가능

| 단계 | 작업 내용 | 파일 |
|------|----------|------|
| 1 | 태그 타입 정의 | `types/tag.ts` |
| 2 | 태그 API 서비스 (CRUD) | `api/services/tagService.ts` |
| 3 | 태그 React Query 훅 (CRUD) | `hooks/queries/useTagQueries.ts` |
| 4 | TagBadge 컴포넌트 | `components/family/TagBadge.tsx` |
| 5 | ColorPicker 컴포넌트 | `components/family/ColorPicker.tsx` |
| 6 | TagManagement 화면 | `components/family/TagManagement.tsx` |
| 7 | 설정 페이지에 태그 관리 링크 추가 | `pages/SettingsPage.tsx` |

**체크리스트**:
- [ ] 태그 생성 동작 확인
- [ ] 태그 수정 (이름, 색상) 동작 확인
- [ ] 태그 삭제 동작 확인
- [ ] 태그 10개 제한 에러 처리
- [ ] PR 생성 (`create-pr` 스킬)

---

### 14.3 Sprint 2-BE: 멤버-태그 연결 백엔드

**목표**: 멤버에 태그 할당/해제 API + 멤버 조회 시 태그 포함

| 단계 | 작업 내용 | 스킬 |
|------|----------|------|
| 1 | FamilyMemberTagMapping 도메인 | `core-tdd` |
| 2 | FamilyMemberTagMappingJpaEntity, Repository | `infra-tdd` |
| 3 | Adapter에 매핑 관련 메서드 추가 | `infra-tdd` |
| 4 | ModifyMemberTagsCommand/UseCase/Service | `core-tdd` |
| 5 | 멤버 태그 할당 API (PUT) | `presentation-tdd` |
| 6 | 기존 멤버 조회 API에 tags 필드 추가 | `presentation-tdd` |
| 7 | DB 마이그레이션 (family_member_tag_mapping) | 수동 |

**체크리스트**:
- [x] 매핑 도메인 테스트 통과
- [x] 멤버 태그 할당 서비스 테스트 통과
- [x] 멤버 조회 시 태그 포함 확인
- [x] 멤버당 10개 태그 제한 테스트
- [x] `./gradlew test` 전체 통과
- [x] Controller Port 의존 제거 리팩토링 완료
- [x] PR 생성 (`create-pr` 스킬) - PR #29

---

### 14.4 Sprint 2-FE: 멤버-태그 연결 프론트

**목표**: 멤버에 태그 할당/표시

| 단계 | 작업 내용 | 파일 |
|------|----------|------|
| 1 | 태그 할당 API 추가 | `api/services/tagService.ts` |
| 2 | 태그 할당 React Query 훅 | `hooks/queries/useTagQueries.ts` |
| 3 | TagSelectModal 컴포넌트 | `components/family/TagSelectModal.tsx` |
| 4 | MemberDetailModal 컴포넌트 | `components/family/MemberDetailModal.tsx` |
| 5 | 멤버 카드에 태그 뱃지 표시 | `pages/HomePage.tsx` |
| 6 | 멤버 클릭 시 MemberDetailModal 표시 | `pages/HomePage.tsx` |

**체크리스트**:
- [ ] 멤버 클릭 → 모달 표시 (404 수정)
- [ ] 모달에서 태그 뱃지 표시
- [ ] "태그 수정" → TagSelectModal 연결
- [ ] 태그 선택/해제 동작 확인
- [ ] 멤버 카드에 태그 뱃지 표시
- [ ] PR 생성 (`create-pr` 스킬)

---

### 14.5 Sprint 3: 스와이프 UX

**목표**: 모바일 친화적 스와이프 인터랙션

| 단계 | 작업 내용 | 파일 |
|------|----------|------|
| 1 | useSwipe 훅 | `hooks/useSwipe.ts` |
| 2 | 멤버 카드에 스와이프 적용 | `pages/HomePage.tsx` |
| 3 | 스와이프 시 "태그 추가" 버튼 노출 | `pages/HomePage.tsx` |
| 4 | TagSelectModal 연결 | `pages/HomePage.tsx` |

**체크리스트**:
- [ ] 좌로 스와이프 → "태그 추가" 버튼 노출
- [ ] 버튼 클릭 → TagSelectModal 표시
- [ ] 스와이프 임계값 적절히 설정
- [ ] 데스크톱에서는 hover로 대체 (선택)
- [ ] PR 생성 (`create-pr` 스킬)

---

### 14.6 Sprint 4: 필터링

**목표**: 태그별 멤버 필터링

| 단계 | 작업 내용 | 파일 |
|------|----------|------|
| 1 | TagFilter 컴포넌트 | `components/family/TagFilter.tsx` |
| 2 | 필터 상태 관리 | `pages/HomePage.tsx` |
| 3 | 필터링 로직 (OR 조건) | `pages/HomePage.tsx` |
| 4 | 필터 결과 표시 (n개 선택됨 · m명) | `pages/HomePage.tsx` |

**체크리스트**:
- [ ] "전체" 칩 → 필터 초기화
- [ ] 태그 칩 토글 선택/해제
- [ ] 복수 선택 시 OR 조건 필터링
- [ ] 필터링된 멤버 수 표시
- [ ] 가로 스크롤 동작 확인
- [ ] PR 생성 (`create-pr` 스킬)

---

## 15. 구현 체크리스트 (스킬별)

### Phase 1: 백엔드 도메인 (core-tdd)

> **스킬**: `core-tdd` - Domain, Command/Query 구현

#### 1.1 FamilyMemberTag 도메인

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/domain/FamilyMemberTag.java`

- [x] 필드 정의 (id, familyId, name, **color**, createdBy, createdAt, modifiedBy, modifiedAt)
- [x] COLOR_PALETTE 상수 정의 (9개 색상)
- [x] `newTag(familyId, name, createdBy)` 팩토리 메서드 (색상 랜덤 배정)
- [x] `withId(...)` 팩토리 메서드
- [x] `rename(newName, modifiedBy)` 비즈니스 메서드
- [x] `changeColor(newColor, modifiedBy)` 비즈니스 메서드
- [x] 색상 유효성 검증 (COLOR_PALETTE에 있는지)
- [x] 단위 테스트: `FamilyMemberTagTest.java`

#### 1.2 FamilyMemberTagMapping 도메인

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/domain/FamilyMemberTagMapping.java`

- [x] 필드 정의 (id, tagId, memberId, createdAt)
- [x] `newMapping(tagId, memberId)` 팩토리 메서드
- [x] `withId(...)` 팩토리 메서드
- [x] 단위 테스트: `FamilyMemberTagMappingTest.java`

---

### Phase 2: 백엔드 인프라 (infra-tdd)

> **스킬**: `infra-tdd` - JpaEntity, Repository, Adapter 구현

#### 2.1 JPA 엔티티

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/out/persistence/FamilyMemberTagJpaEntity.java`

- [x] `@Entity`, `@Table(name = "family_member_tag")` 설정
- [x] 컬럼 매핑 (id, family_id, name, **color**, created_by, created_at, modified_by, modified_at)
- [x] `@SQLRestriction` 없음 (soft delete 미적용)
- [x] `from(FamilyMemberTag)` 변환 메서드
- [x] `toFamilyMemberTag()` 변환 메서드
- [x] 통합 테스트

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/out/persistence/FamilyMemberTagMappingJpaEntity.java`

- [x] `@Entity`, `@Table(name = "family_member_tag_mapping")` 설정
- [x] 컬럼 매핑 (id, tag_id, member_id, created_at)
- [x] `from(FamilyMemberTagMapping)` 변환 메서드
- [x] `toFamilyMemberTagMapping()` 변환 메서드

#### 2.2 JPA Repository

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/out/persistence/FamilyMemberTagJpaRepository.java`

- [x] `findAllByFamilyId(Long familyId)` - 태그 목록 조회
- [x] `findByFamilyIdAndName(Long familyId, String name)` - 이름 중복 체크
- [x] `countByFamilyId(Long familyId)` - 태그 수 카운트
- [x] `findByIdAndFamilyId(Long id, Long familyId)` - 태그 단건 조회

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/out/persistence/FamilyMemberTagMappingJpaRepository.java`

- [ ] `findAllByTagId(Long tagId)` - 태그에 할당된 멤버 조회
- [x] `findAllByMemberId(Long memberId)` - 멤버에 할당된 태그 조회
- [ ] `deleteAllByTagId(Long tagId)` - 태그 삭제 시 매핑 삭제
- [x] `deleteAllByMemberId(Long memberId)` - 멤버 삭제 시 매핑 삭제
- [x] `countByTagId(Long tagId)` - 태그별 멤버 수 카운트

#### 2.3 Adapter (Port 구현)

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/out/persistence/FamilyMemberTagAdapter.java`

**Out Port 구현**:
- [x] `SaveFamilyMemberTagPort.save(FamilyMemberTag)` → 저장/수정
- [x] `FindFamilyMemberTagPort.findById(Long)` → 단건 조회
- [x] `FindFamilyMemberTagPort.findAllByFamilyId(Long)` → 목록 조회
- [x] `FindFamilyMemberTagPort.findByFamilyIdAndName(Long, String)` → 이름 중복 체크
- [x] `FindFamilyMemberTagPort.countByFamilyId(Long)` → 태그 수 카운트
- [x] `DeleteFamilyMemberTagPort.deleteById(Long)` → 삭제
- [x] `SaveFamilyMemberTagMappingPort.saveAll(List<FamilyMemberTagMapping>)` → 매핑 저장
- [x] `DeleteFamilyMemberTagMappingPort.deleteAllByMemberId(Long)` → 멤버 매핑 삭제
- [x] `FindFamilyMemberTagMappingPort.findAllByMemberId(Long)` → 멤버별 태그 조회
- [x] `FindFamilyMemberTagMappingPort.countByTagId(Long)` → 태그별 멤버 수

- [x] 통합 테스트: `FamilyMemberTagAdapterTest.java`

#### 2.4 DB 마이그레이션

**파일**: `be/src/main/resources/db/migration/V{N}__add_family_member_tag.sql`

- [x] `family_member_tag` 테이블 생성
- [x] `family_member_tag_mapping` 테이블 생성
- [x] 인덱스 생성
- [x] FK 제약조건 (CASCADE DELETE)
- [x] UNIQUE 제약조건

---

### Phase 3: 백엔드 유스케이스/서비스 (core-tdd)

> **스킬**: `core-tdd` - UseCase, Service, Command/Query 구현

#### 3.1 태그 생성

**Command**: `SaveFamilyMemberTagCommand.java`
- [x] familyId, name 필드
- [x] `@NotNull`, `@Size(min=1, max=10)`, `@Pattern` 검증

**UseCase**: `SaveFamilyMemberTagUseCase.java`
- [x] `Long save(Long currentUserId, SaveFamilyMemberTagCommand command)`

**Service**: `SaveFamilyMemberTagService.java`
- [x] OWNER 권한 검증
- [x] 태그 수 제한 검증 (10개)
- [x] 이름 중복 검증
- [x] 태그 생성 및 저장
- [x] 단위 테스트: `SaveFamilyMemberTagServiceTest.java`

#### 3.2 태그 목록 조회

**Query**: `FindFamilyMemberTagsQuery.java`
- [x] familyId 필드

**UseCase**: `FindFamilyMemberTagUseCase.java`
- [x] `List<FamilyMemberTagInfo> findAll(Long currentUserId, FindFamilyMemberTagsQuery query)`

**Service**: `FindFamilyMemberTagService.java`
- [x] Family 접근 권한 검증
- [x] 태그 목록 조회 + 각 태그별 멤버 수
- [x] 단위 테스트: `FindFamilyMemberTagServiceTest.java`

**DTO**: `FamilyMemberTagInfo.java`
- [x] id, name, memberCount, createdAt

#### 3.3 태그 수정

**Command**: `ModifyFamilyMemberTagCommand.java`
- [x] familyId, tagId, name 필드
- [x] 검증 어노테이션

**UseCase**: `ModifyFamilyMemberTagUseCase.java`
- [x] `void modify(Long currentUserId, ModifyFamilyMemberTagCommand command)`

**Service**: `ModifyFamilyMemberTagService.java`
- [x] OWNER 권한 검증
- [x] 태그 존재 및 Family 일치 검증
- [x] 이름 중복 검증 (본인 제외)
- [x] 태그 이름 수정
- [x] 단위 테스트: `ModifyFamilyMemberTagServiceTest.java`

#### 3.4 태그 삭제

**Command**: `DeleteFamilyMemberTagCommand.java`
- [x] familyId, tagId 필드

**UseCase**: `DeleteFamilyMemberTagUseCase.java`
- [x] `void delete(Long currentUserId, DeleteFamilyMemberTagCommand command)`

**Service**: `DeleteFamilyMemberTagService.java`
- [x] OWNER 권한 검증
- [x] 태그 존재 및 Family 일치 검증
- [x] 태그 삭제 (CASCADE로 매핑 자동 삭제)
- [x] 단위 테스트: `DeleteFamilyMemberTagServiceTest.java`

#### 3.5 멤버 태그 할당

**Command**: `ModifyMemberTagsCommand.java`
- [x] familyId, memberId, tagIds 필드

**UseCase**: `ModifyMemberTagsUseCase.java`
- [x] `MemberTagsInfo modify(Long currentUserId, ModifyMemberTagsCommand command)`

**Service**: `ModifyMemberTagsService.java`
- [x] OWNER 권한 검증
- [x] 멤버 존재 및 Family 일치 검증
- [x] tagIds 전체 유효성 검증 (존재 여부, Family 일치)
- [x] 기존 매핑 삭제 후 새 매핑 저장 (전체 교체 방식)
- [x] 단위 테스트: `ModifyMemberTagsServiceTest.java`

**DTO**: `MemberTagsInfo.java`
- [x] memberId, memberName, tags (id, name 리스트)

---

### Phase 4: 백엔드 프레젠테이션 (presentation-tdd)

> **스킬**: `presentation-tdd` - Controller, Request/Response DTO 구현

#### 4.1 태그 Controller

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/in/web/`
- [x] `SaveFamilyMemberTagController.java`
- [x] `FindFamilyMemberTagController.java`
- [x] `ModifyFamilyMemberTagController.java`
- [x] `DeleteFamilyMemberTagController.java`

**API 엔드포인트**:

| HTTP | Path | Controller | 상태 |
|------|------|--------|------|
| GET | `/api/families/{familyId}/tags` | `FindFamilyMemberTagController` | ✅ |
| POST | `/api/families/{familyId}/tags` | `SaveFamilyMemberTagController` | ✅ |
| PUT | `/api/families/{familyId}/tags/{tagId}` | `ModifyFamilyMemberTagController` | ✅ |
| DELETE | `/api/families/{familyId}/tags/{tagId}` | `DeleteFamilyMemberTagController` | ✅ |
| PUT | `/api/families/{familyId}/members/{memberId}/tags` | `ModifyMemberTagsController` | ✅ |

#### 4.2 Request DTO

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/in/web/request/`

- [x] `SaveFamilyMemberTagRequest.java` - name (color는 서버에서 랜덤 배정)
- [x] `ModifyFamilyMemberTagRequest.java` - name, **color** (선택)
- [x] `ModifyMemberTagsRequest.java` - tagIds

#### 4.3 Response DTO

**파일**: `be/src/main/java/io/jhchoe/familytree/core/family/adapter/in/web/response/`

- [x] `FamilyMemberTagResponse.java` - id, name, **color**, memberCount, createdAt
- [x] `FamilyMemberTagListResponse.java` - tags, totalCount, maxCount
- [x] `ModifyMemberTagsResponse.java` - memberId, memberName, tags (id, name, **color**)
- [x] `TagInfo` record - id, name, **color** (멤버 조회 시 태그 정보용, FamilyMemberWithRelationshipResponse 내부)

#### 4.4 기존 API 수정

**파일 수정**: `FamilyMemberWithRelationshipResponse.java`
- [x] `tags` 필드 추가 (List<TagInfo>)

**파일 수정**: `FindFamilyHomeMemberController.java`
- [x] 멤버 조회 시 태그 정보 함께 조회

#### 4.5 REST Docs 테스트

**파일**: `be/src/test/java/io/jhchoe/familytree/core/family/adapter/in/web/`
- `SaveFamilyMemberTagControllerTest.java`
- `FindFamilyMemberTagControllerTest.java`
- `ModifyFamilyMemberTagControllerTest.java`
- `DeleteFamilyMemberTagControllerTest.java`

- [x] 태그 목록 조회 API 문서
- [x] 태그 생성 API 문서
- [x] 태그 수정 API 문서
- [x] 태그 삭제 API 문서
- [ ] 멤버 태그 할당 API 문서
- [x] 에러 케이스 문서

---

### Phase 5: 프론트엔드

#### 5.1 타입 정의

**파일**: `fe/src/types/tag.ts`

```typescript
export interface Tag {
  id: number;
  name: string;
  color: string;  // HEX 색상 코드
  memberCount: number;
  createdAt: string;
}

export interface TagSimple {
  id: number;
  name: string;
  color: string;
}

export interface TagListResponse {
  tags: Tag[];
  totalCount: number;
  maxCount: number;
}

export interface MemberTags {
  memberId: number;
  memberName: string;
  tags: TagSimple[];
}

// 색상 팔레트 상수
export const TAG_COLOR_PALETTE = [
  '#E3E2E0', '#EEE0DA', '#FADEC9', '#FDECC8', '#DBEDDB',
  '#D3E5EF', '#E8DEEE', '#F5E0E9', '#FFE2DD'
] as const;

export type TagColor = typeof TAG_COLOR_PALETTE[number];
```

#### 5.2 API 서비스

**파일**: `fe/src/api/services/tagService.ts`

- [ ] `findTags(familyId)` - GET /api/families/{familyId}/tags
- [ ] `saveTag(familyId, name)` - POST /api/families/{familyId}/tags
- [ ] `modifyTag(familyId, tagId, name)` - PUT /api/families/{familyId}/tags/{tagId}
- [ ] `deleteTag(familyId, tagId)` - DELETE /api/families/{familyId}/tags/{tagId}
- [ ] `modifyMemberTags(familyId, memberId, tagIds)` - PUT /api/families/{familyId}/members/{memberId}/tags

#### 5.3 React Query 훅

**파일**: `fe/src/hooks/queries/useTagQueries.ts`

- [ ] `useTagsQuery(familyId)` - 태그 목록 조회
- [ ] `useSaveTagMutation()` - 태그 생성
- [ ] `useModifyTagMutation()` - 태그 수정
- [ ] `useDeleteTagMutation()` - 태그 삭제
- [ ] `useModifyMemberTagsMutation()` - 멤버 태그 할당

#### 5.4 컴포넌트

**파일**: `fe/src/components/family/TagBadge.tsx`

- [ ] 태그 뱃지 UI (**색상 배경**, 작은 글씨)
- [ ] 색상에 따른 텍스트 색상 조정 (contrast)
- [ ] 선택적 삭제 버튼 (X)

**파일**: `fe/src/components/family/TagSelectModal.tsx`

- [ ] 태그 목록 (체크박스 + **색상 아이콘**)
- [ ] 새 태그 만들기 입력
- [ ] 선택/해제 토글
- [ ] 확인/취소 버튼
- [ ] 가나다순 정렬

**파일**: `fe/src/components/family/TagManagement.tsx`

- [ ] 태그 목록 (이름, **색상**, 멤버 수)
- [ ] 태그 수정/삭제 메뉴
- [ ] **색상 변경** (ColorPicker 연결)
- [ ] 새 태그 만들기
- [ ] 사용량 표시 (n/10개)
- [ ] 가나다순 정렬

**파일**: `fe/src/components/family/ColorPicker.tsx` (신규)

- [ ] 9가지 색상 팔레트 표시
- [ ] 현재 선택된 색상 표시
- [ ] 색상 클릭 시 선택 이벤트

**파일**: `fe/src/components/family/MemberDetailModal.tsx` (신규)

- [ ] 멤버 기본 정보 표시 (이름, 생일, 관계)
- [ ] 아바타 크게 표시
- [ ] 태그 뱃지 목록 (색상 적용)
- [ ] OWNER일 때만 "태그 수정" 버튼 표시
- [ ] TagSelectModal 연결

**파일**: `fe/src/components/family/TagFilter.tsx` (신규)

- [ ] 태그 칩 목록 (가로 스크롤)
- [ ] "전체" 칩 (필터 초기화)
- [ ] 각 태그 칩에 색상 아이콘 표시
- [ ] 선택 상태 표시 (체크 아이콘 또는 테두리 강조)
- [ ] 복수 선택 지원 (토글 방식)
- [ ] 선택된 태그 수 + 필터링된 멤버 수 표시

#### 5.5 스와이프 기능

**파일**: `fe/src/hooks/useSwipe.ts`

- [ ] 터치 이벤트 핸들링 (touchstart, touchmove, touchend)
- [ ] 스와이프 방향 감지
- [ ] 스와이프 거리 임계값

**파일 수정**: `fe/src/pages/HomePage.tsx`

- [ ] 멤버 카드에 스와이프 적용
- [ ] 스와이프 시 "태그 추가" 액션 버튼 노출
- [ ] 태그 선택 모달 연결
- [ ] 멤버 카드에 태그 뱃지 표시 (색상 적용)
- [ ] **멤버 클릭 시 MemberDetailModal 표시** (기존 404 수정)
- [ ] 가나다순 정렬된 태그 표시
- [ ] **TagFilter 컴포넌트 추가** (멤버 목록 상단)
- [ ] **필터 상태 관리** (selectedTagIds: number[])
- [ ] **필터링 로직** (OR 조건 - 선택된 태그 중 하나라도 포함된 멤버)

---

### Phase 6: 테스트 및 검증

#### 6.1 백엔드 테스트

- [ ] `./gradlew test` 전체 통과
- [ ] `./gradlew build` 전체 통과
- [ ] API 문서 생성 확인

#### 6.2 프론트엔드 테스트

- [ ] 컴포넌트 렌더링 테스트
- [ ] 스와이프 동작 테스트 (모바일)

#### 6.3 통합 테스트

- [ ] 태그 생성 → 멤버에 할당 → 조회 플로우
- [ ] 태그 삭제 시 멤버 연결 해제 확인
- [ ] 권한 검증 (OWNER 외 사용자 차단)

---

## 16. 향후 확장 (Out of Scope)

다음 기능들은 이번 PRD 범위에서 제외하며, 별도 PRD로 진행:

| 기능 | 설명 |
|------|------|
| 태그 알림 | 특정 태그의 멤버들에게만 알림 |
| 태그 순서 | 드래그로 태그 순서 변경 (현재는 가나다순 고정) |
| 다중 태그 한번에 할당 | 여러 멤버에 한번에 같은 태그 할당 |

> **Note**: 태그 색상, 태그별 필터링 기능은 이번 PRD 범위에 포함됨
